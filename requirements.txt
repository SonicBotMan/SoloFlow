# SoloFlow 依赖

# 核心
openai>=1.12.0
pyyaml>=6.0
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0

# 数据库
aiosqlite>=0.20.0

# 工具
python-dotenv>=1.0.0

# MCP 支持（可选，用于 MCPDriver stdio 模式需要 Node.js 环境）
# 如果使用 Python MCP SDK:
# mcp>=1.0.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
